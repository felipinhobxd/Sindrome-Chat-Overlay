package com.sindromegames.chatoverlay.providers;

public enum YouTubeMode {
    STOPPED,
    CONNECTING,
    COMPATIBILITY,
    OFFICIAL_STREAM,
    OFFICIAL_POLLING,
    INVALID_KEY,
    COMPATIBILITY_FALLBACK
}

